[
  {
    "id": "fn.replace-ms-star",
    "description": "Microsoft example: REPLACE(\"abcdefghijk\", 6, 5, \"*\") → \"abcde*k\"",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abcdefghijk\", 6, 5, \"*\")",
    "expected": { "text": "abcde*k" }
  },
  {
    "id": "fn.replace-ms-year",
    "description": "Microsoft example: REPLACE(\"2009\", 3, 2, \"10\") → \"2010\"",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"2009\", 3, 2, \"10\")",
    "expected": { "text": "2010" }
  },
  {
    "id": "fn.replace-ms-at",
    "description": "Microsoft example: REPLACE(\"123456\", 1, 3, \"@\") → \"@456\"",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"123456\", 1, 3, \"@\")",
    "expected": { "text": "@456" }
  },
  {
    "id": "fn.replace-start-1",
    "description": "start_num 1 (1-based) replaces the first character",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\", 1, 1, \"X\")",
    "expected": { "text": "Xbc" }
  },
  {
    "id": "fn.replace-start-2",
    "description": "start_num 2 replaces the second character",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\", 2, 1, \"X\")",
    "expected": { "text": "aXc" }
  },
  {
    "id": "fn.replace-start-3",
    "description": "start_num 3 replaces the last character of a 3-char string",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\", 3, 1, \"X\")",
    "expected": { "text": "abX" }
  },
  {
    "id": "fn.replace-num-0-insert",
    "description": "num_chars 0 inserts new_text at start_num without deleting",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\", 2, 0, \"X\")",
    "expected": { "text": "aXbc" }
  },
  {
    "id": "fn.replace-num-0-prefix",
    "description": "num_chars 0 at start_num 1 prefixes new_text",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\", 1, 0, \"X\")",
    "expected": { "text": "Xabc" }
  },
  {
    "id": "fn.replace-num-0-end",
    "description": "num_chars 0 at LEN+1 appends",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\", 4, 0, \"X\")",
    "expected": { "text": "abcX" }
  },
  {
    "id": "fn.replace-num-all",
    "description": "num_chars equal to LEN replaces the whole string",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\", 1, 3, \"X\")",
    "expected": { "text": "X" }
  },
  {
    "id": "fn.replace-num-past-end",
    "description": "num_chars past the end replaces the remainder",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\", 2, 10, \"X\")",
    "expected": { "text": "aX" }
  },
  {
    "id": "fn.replace-empty-new",
    "description": "Empty new_text deletes the span",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\", 2, 1, \"\")",
    "expected": { "text": "ac" }
  },
  {
    "id": "fn.replace-empty-new-all",
    "description": "Empty new_text with a full-span delete yields \"\"",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\", 1, 3, \"\")",
    "expected": { "text": "" }
  },
  {
    "id": "fn.replace-empty-new-insert",
    "description": "Empty new_text and num_chars 0 is a no-op",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\", 2, 0, \"\")",
    "expected": { "text": "abc" }
  },
  {
    "id": "fn.replace-start-past-end",
    "description": "start_num = LEN+1 appends new_text (no error)",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\", 4, 1, \"X\")",
    "expected": { "text": "abcX" }
  },
  {
    "id": "fn.replace-start-far-past",
    "description": "start_num far past LEN still appends",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\", 100, 5, \"X\")",
    "expected": { "text": "abcX" }
  },
  {
    "id": "fn.replace-start-0",
    "description": "start_num 0 is #VALUE! (positions are 1-based)",
    "tags": ["text", "replace", "errors"],
    "formula": "=REPLACE(\"abc\", 0, 1, \"X\")",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.replace-start-neg",
    "description": "Negative start_num is #VALUE!",
    "tags": ["text", "replace", "errors"],
    "formula": "=REPLACE(\"abc\", -1, 1, \"X\")",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.replace-num-neg",
    "description": "Negative num_chars is #VALUE!",
    "tags": ["text", "replace", "errors"],
    "formula": "=REPLACE(\"abc\", 1, -1, \"X\")",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.replace-start-trunc",
    "description": "start_num 2.9 truncates toward zero to 2",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\", 2.9, 1, \"X\")",
    "expected": { "text": "aXc" }
  },
  {
    "id": "fn.replace-start-trunc-zero",
    "description": "start_num 0.9 truncates to 0 → #VALUE!",
    "tags": ["text", "replace", "errors"],
    "formula": "=REPLACE(\"abc\", 0.9, 1, \"X\")",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.replace-num-trunc",
    "description": "num_chars 1.9 truncates toward zero to 1",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\", 1, 1.9, \"X\")",
    "expected": { "text": "Xbc" }
  },
  {
    "id": "fn.replace-number",
    "description": "Numbers coerce to plain text; result stays text",
    "tags": ["text", "replace", "coercion"],
    "quirks": ["equality-vs-arithmetic-coercion"],
    "formula": "=REPLACE(2009, 3, 2, 10)",
    "expected": { "text": "2010" }
  },
  {
    "id": "fn.replace-bool-old",
    "description": "TRUE coerces to the text \"TRUE\"",
    "tags": ["text", "replace", "coercion"],
    "formula": "=REPLACE(TRUE, 1, 1, \"X\")",
    "expected": { "text": "XRUE" }
  },
  {
    "id": "fn.replace-start-true",
    "description": "TRUE coerces to start_num 1",
    "tags": ["text", "replace", "coercion"],
    "quirks": ["bool-number-equality"],
    "formula": "=REPLACE(\"abc\", TRUE, 1, \"X\")",
    "expected": { "text": "Xbc" }
  },
  {
    "id": "fn.replace-start-false",
    "description": "FALSE coerces to start_num 0 → #VALUE!",
    "tags": ["text", "replace", "errors", "coercion"],
    "quirks": ["bool-number-equality"],
    "formula": "=REPLACE(\"abc\", FALSE, 1, \"X\")",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.replace-num-false",
    "description": "FALSE coerces to num_chars 0 (insert)",
    "tags": ["text", "replace", "coercion"],
    "quirks": ["bool-number-equality"],
    "formula": "=REPLACE(\"abc\", 2, FALSE, \"X\")",
    "expected": { "text": "aXbc" }
  },
  {
    "id": "fn.replace-num-true",
    "description": "TRUE coerces to num_chars 1",
    "tags": ["text", "replace", "coercion"],
    "quirks": ["bool-number-equality"],
    "formula": "=REPLACE(\"abc\", 2, TRUE, \"X\")",
    "expected": { "text": "aXc" }
  },
  {
    "id": "fn.replace-start-text-num",
    "description": "Numeric text start_num is coerced",
    "tags": ["text", "replace", "coercion"],
    "quirks": ["equality-vs-arithmetic-coercion"],
    "formula": "=REPLACE(\"abc\", \"2\", 1, \"X\")",
    "expected": { "text": "aXc" }
  },
  {
    "id": "fn.replace-start-text-bad",
    "description": "Non-numeric start_num is #VALUE!",
    "tags": ["text", "replace", "errors"],
    "formula": "=REPLACE(\"abc\", \"x\", 1, \"X\")",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.replace-blank-old",
    "description": "Blank old_text cell coerces to \"\"",
    "tags": ["text", "replace", "empty"],
    "quirks": ["empty-cell-duality"],
    "formula": "=REPLACE(A1, 1, 0, \"X\")",
    "expected": { "text": "X" },
    "workbook": {
      "sheets": [{ "name": "Sheet1", "cells": {} }]
    }
  },
  {
    "id": "fn.replace-blank-start",
    "description": "Blank start_num cell coerces to 0 → #VALUE!",
    "tags": ["text", "replace", "errors", "empty"],
    "quirks": ["empty-cell-duality"],
    "formula": "=REPLACE(\"abc\", A1, 1, \"X\")",
    "expected": { "error": "#VALUE!" },
    "workbook": {
      "sheets": [{ "name": "Sheet1", "cells": {} }]
    }
  },
  {
    "id": "fn.replace-blank-num",
    "description": "Blank num_chars cell coerces to 0 (insert)",
    "tags": ["text", "replace", "empty"],
    "quirks": ["empty-cell-duality"],
    "formula": "=REPLACE(\"abc\", 2, A1, \"X\")",
    "expected": { "text": "aXbc" },
    "workbook": {
      "sheets": [{ "name": "Sheet1", "cells": {} }]
    }
  },
  {
    "id": "fn.replace-empty-old",
    "description": "REPLACE(\"\", 1, 0, \"X\") inserts into empty text",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"\", 1, 0, \"X\")",
    "expected": { "text": "X" }
  },
  {
    "id": "fn.replace-empty-old-num",
    "description": "REPLACE(\"\", 1, 1, \"X\") on empty text still yields new_text",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"\", 1, 1, \"X\")",
    "expected": { "text": "X" }
  },
  {
    "id": "fn.replace-unicode-cafe",
    "description": "Precomposed é is one Unicode scalar (same as LEN/MID)",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"café\", 4, 1, \"e\")",
    "expected": { "text": "cafe" }
  },
  {
    "id": "fn.replace-unicode-cjk",
    "description": "CJK ideographs are one scalar each",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"日本語\", 2, 1, \"X\")",
    "expected": { "text": "日X語" }
  },
  {
    "id": "fn.replace-emoji-scalar",
    "description": "Surrogate-pair emoji is one character (Excel Compatibility Version 2; matches LEN/MID chars). Version 1 counted UTF-16 units (😀=2) and is not the calc-core default.",
    "tags": ["text", "replace"],
    "notes": "Microsoft Compatibility Version 2: LEN/MID/FIND/SEARCH/REPLACE count a surrogate pair as one character. Version 1 (legacy UTF-16) is deferred.",
    "formula": "=REPLACE(\"a😀b\", 2, 1, \"X\")",
    "expected": { "text": "aXb" }
  },
  {
    "id": "fn.replace-emoji-after",
    "description": "Character after a surrogate-pair emoji is start_num 3 under Version 2",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"a😀b\", 3, 1, \"X\")",
    "expected": { "text": "a😀X" }
  },
  {
    "id": "fn.replace-combining-mark",
    "description": "Combining marks stay separate characters (Excel Version 2; not clustered into a grapheme)",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(A1, 2, 1, \"\")",
    "expected": { "text": "e" },
    "workbook": {
      "sheets": [
        {
          "name": "Sheet1",
          "cells": {
            "A1": { "text": "e\u0301" }
          }
        }
      ]
    }
  },
  {
    "id": "fn.replace-combining-base",
    "description": "Replacing the base letter of e+combining-acute keeps the mark",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(A1, 1, 1, \"o\")",
    "expected": { "text": "o\u0301" },
    "workbook": {
      "sheets": [
        {
          "name": "Sheet1",
          "cells": {
            "A1": { "text": "e\u0301" }
          }
        }
      ]
    }
  },
  {
    "id": "fn.replace-arity-0",
    "description": "REPLACE() is #VALUE!",
    "tags": ["text", "replace", "errors"],
    "formula": "=REPLACE()",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.replace-arity-3",
    "description": "Three arguments is #VALUE! (all four are required)",
    "tags": ["text", "replace", "errors"],
    "formula": "=REPLACE(\"abc\", 1, 1)",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.replace-arity-5",
    "description": "Five arguments is #VALUE!",
    "tags": ["text", "replace", "errors"],
    "formula": "=REPLACE(\"abc\", 1, 1, \"X\", 1)",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.replace-err-old",
    "description": "Error in old_text propagates",
    "tags": ["text", "replace", "errors"],
    "quirks": ["error-precedence"],
    "formula": "=REPLACE(1/0, 1, 1, \"X\")",
    "expected": { "error": "#DIV/0!" }
  },
  {
    "id": "fn.replace-err-start",
    "description": "Error in start_num propagates",
    "tags": ["text", "replace", "errors"],
    "quirks": ["error-precedence"],
    "formula": "=REPLACE(\"abc\", 1/0, 1, \"X\")",
    "expected": { "error": "#DIV/0!" }
  },
  {
    "id": "fn.replace-err-num",
    "description": "Error in num_chars propagates",
    "tags": ["text", "replace", "errors"],
    "quirks": ["error-precedence"],
    "formula": "=REPLACE(\"abc\", 1, 1/0, \"X\")",
    "expected": { "error": "#DIV/0!" }
  },
  {
    "id": "fn.replace-err-new",
    "description": "Error in new_text propagates",
    "tags": ["text", "replace", "errors"],
    "quirks": ["error-precedence"],
    "formula": "=REPLACE(\"abc\", 1, 1, 1/0)",
    "expected": { "error": "#DIV/0!" }
  },
  {
    "id": "fn.replace-err-left-wins",
    "description": "Left-to-right: old_text error beats a later #N/A",
    "tags": ["text", "replace", "errors"],
    "quirks": ["error-precedence"],
    "formula": "=REPLACE(#DIV/0!, #N/A, 1, \"X\")",
    "expected": { "error": "#DIV/0!" }
  },
  {
    "id": "fn.replace-num-trunc-zero",
    "description": "num_chars 0.9 truncates toward zero to 0 (insert)",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\", 2, 0.9, \"X\")",
    "expected": { "text": "aXbc" }
  },
  {
    "id": "fn.replace-num-trunc-neg-frac",
    "description": "num_chars -0.9 truncates toward zero to 0 (insert, not #VALUE!)",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\", 2, -0.9, \"X\")",
    "expected": { "text": "aXbc" }
  },
  {
    "id": "fn.replace-start-percent",
    "description": "200% coerces to start_num 2",
    "tags": ["text", "replace", "coercion"],
    "formula": "=REPLACE(\"abc\", 200%, 1, \"X\")",
    "expected": { "text": "aXc" }
  },
  {
    "id": "fn.replace-start-huge",
    "description": "1E+20 start_num is past LEN and appends",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\", 1E+20, 1, \"X\")",
    "expected": { "text": "abcX" }
  },
  {
    "id": "fn.replace-grow",
    "description": "new_text longer than the span grows the string",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\", 2, 1, \"XYZ\")",
    "expected": { "text": "aXYZc" }
  },
  {
    "id": "fn.replace-space",
    "description": "A space is a character that can be overwritten",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"a b\", 2, 1, \"-\")",
    "expected": { "text": "a-b" }
  },
  {
    "id": "fn.replace-omit-start",
    "description": "Omitted start_num is blank → 0 → #VALUE!",
    "tags": ["text", "replace", "errors", "empty"],
    "quirks": ["empty-cell-duality"],
    "formula": "=REPLACE(\"abc\",, 1, \"X\")",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.replace-omit-num",
    "description": "Omitted num_chars is blank → 0 (insert at start_num)",
    "tags": ["text", "replace", "empty"],
    "quirks": ["empty-cell-duality"],
    "formula": "=REPLACE(\"abc\", 1,, \"X\")",
    "expected": { "text": "Xabc" }
  },
  {
    "id": "fn.replace-omit-new",
    "description": "Omitted new_text is blank → delete the span",
    "tags": ["text", "replace", "empty"],
    "quirks": ["empty-cell-duality"],
    "formula": "=REPLACE(\"abc\", 1, 1,)",
    "expected": { "text": "bc" }
  },
  {
    "id": "fn.replace-bool-new",
    "description": "TRUE as new_text coerces to \"TRUE\"",
    "tags": ["text", "replace", "coercion"],
    "quirks": ["bool-number-equality"],
    "formula": "=REPLACE(\"abc\", 2, 1, TRUE)",
    "expected": { "text": "aTRUEc" }
  },
  {
    "id": "fn.replace-named",
    "description": "REPLACE of a defined name referring to a cell",
    "tags": ["text", "replace", "named"],
    "formula": "=REPLACE(Title, 2, 3, \"i\")",
    "expected": { "text": "Hio" },
    "workbook": {
      "sheets": [
        {
          "name": "Sheet1",
          "cells": {
            "A1": { "text": "Hello" }
          }
        }
      ],
      "names": [{ "name": "Title", "refers_to": "Sheet1!A1" }]
    }
  },
  {
    "id": "fn.replace-formula-cell",
    "description": "REPLACE of a formula cell that yields text",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(A1, 1, 1, \"Y\")",
    "expected": { "text": "Yello" },
    "workbook": {
      "sheets": [
        {
          "name": "Sheet1",
          "cells": {
            "A1": { "formula": "=\"Hello\"" }
          }
        }
      ]
    }
  },
  {
    "id": "fn.replace-nested-left",
    "description": "REPLACE(LEFT(\"hello\", 3), 2, 1, \"X\") → \"hXo\"",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(LEFT(\"hello\", 3), 2, 1, \"X\")",
    "expected": { "text": "hXo" }
  },
  {
    "id": "fn.replace-len-last",
    "description": "start_num = LEN(old_text) replaces the last character",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\", LEN(\"abc\"), 1, \"Z\")",
    "expected": { "text": "abZ" }
  },
  {
    "id": "fn.replace-len-after",
    "description": "LEN after a one-char delete is LEN-1",
    "tags": ["text", "replace"],
    "formula": "=LEN(REPLACE(\"abc\", 2, 1, \"\"))",
    "expected": { "number": 2 }
  },
  {
    "id": "fn.replace-exact-noop",
    "description": "Empty insert at start is a no-op (EXACT)",
    "tags": ["text", "replace"],
    "formula": "=EXACT(REPLACE(\"abc\", 1, 0, \"\"), \"abc\")",
    "expected": { "bool": true }
  },
  {
    "id": "fn.replace-concat",
    "description": "REPLACE of concatenated pieces uses the joined text",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(\"abc\"&\"def\", 4, 1, \"X\")",
    "expected": { "text": "abcXef" }
  },
  {
    "id": "fn.replace-array-literal",
    "description": "REPLACE of an array literal scalarizes to the top-left",
    "tags": ["text", "replace"],
    "formula": "=REPLACE({\"abc\",\"xyz\"}, 1, 1, \"X\")",
    "expected": { "text": "Xbc" }
  },
  {
    "id": "fn.replace-implicit-intersect",
    "description": "REPLACE(A1:A3, …) from host B2 uses implicit intersection (same row → A2)",
    "tags": ["text", "replace"],
    "quirks": ["implicit-intersection"],
    "at": "Sheet1!B2",
    "formula": "=REPLACE(A1:A3, 1, 1, \"X\")",
    "expected": { "text": "XWO" },
    "workbook": {
      "sheets": [
        {
          "name": "Sheet1",
          "cells": {
            "A1": { "text": "ONE" },
            "A2": { "text": "TWO" },
            "A3": { "text": "THREE" }
          }
        }
      ]
    }
  },
  {
    "id": "fn.replace-variation-selector",
    "description": "Variation selector is its own scalar (Compat v2; not clustered into a grapheme)",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(A1, 2, 1, \"X\")",
    "expected": { "text": "aXb" },
    "workbook": {
      "sheets": [
        {
          "name": "Sheet1",
          "cells": {
            "A1": { "text": "a\ufe0fb" }
          }
        }
      ]
    }
  },
  {
    "id": "fn.replace-emoji-equal-width",
    "description": "Replacing one surrogate-pair emoji with another is still one Compat v2 character",
    "tags": ["text", "replace"],
    "formula": "=REPLACE(A1, 2, 1, B1)",
    "expected": { "text": "a🎉b" },
    "workbook": {
      "sheets": [
        {
          "name": "Sheet1",
          "cells": {
            "A1": { "text": "a😀b" },
            "B1": { "text": "🎉" }
          }
        }
      ]
    }
  },
  {
    "id": "fn.replace-iferror",
    "description": "IFERROR wraps start_num 0 → #VALUE!",
    "tags": ["text", "replace", "errors"],
    "formula": "=IFERROR(REPLACE(\"abc\", 0, 1, \"X\"), \"bad\")",
    "expected": { "text": "bad" }
  },
  {
    "id": "fn.replace-err-na",
    "description": "NA() in start_num propagates #N/A",
    "tags": ["text", "replace", "errors"],
    "quirks": ["error-precedence"],
    "formula": "=REPLACE(\"abc\", NA(), 1, \"X\")",
    "expected": { "error": "#N/A" }
  },
  {
    "id": "fn.replace-replaceb-name",
    "description": "REPLACEB is a different function and is not implemented (#NAME!)",
    "tags": ["text", "replace", "errors"],
    "formula": "=REPLACEB(\"abc\", 1, 1, \"X\")",
    "expected": { "error": "#NAME!" }
  },
  {
    "id": "fn.replace-map",
    "description": "MAP applies REPLACE to each text cell",
    "tags": ["text", "replace", "map"],
    "formula": "=TEXTJOIN(\",\", TRUE, MAP({\"abc\";\"def\"}, LAMBDA(x, REPLACE(x, 1, 1, \"X\"))))",
    "expected": { "text": "Xbc,Xef" }
  },
  {
    "id": "fn.replace-date-format-cell",
    "description": "REPLACE of a date-formatted cell uses the displayed locale string in live Excel. This engine has no cell number format.",
    "tags": ["text", "replace"],
    "ignore": "Cell number formats are not modeled. Do not invent a date-string golden. REPLACE(DATE(...), n, …) uses General text of the serial.",
    "formula": "=REPLACE(A1, 1, 1, \"X\")",
    "expected": { "text": "X" },
    "workbook": {
      "sheets": [
        {
          "name": "Sheet1",
          "cells": {
            "A1": { "number": 45321 }
          }
        }
      ]
    }
  }
]

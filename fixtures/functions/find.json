[
  {
    "id": "fn.find-basic",
    "description": "FIND(\"M\", \"Miriam McGovern\") is 1 (Microsoft example)",
    "tags": ["text", "find"],
    "formula": "=FIND(\"M\", \"Miriam McGovern\")",
    "expected": { "number": 1 }
  },
  {
    "id": "fn.find-second-m",
    "description": "FIND(\"m\", \"Miriam McGovern\") is 6 (Microsoft example)",
    "tags": ["text", "find"],
    "formula": "=FIND(\"m\", \"Miriam McGovern\")",
    "expected": { "number": 6 }
  },
  {
    "id": "fn.find-start-num",
    "description": "FIND(\"M\", \"Miriam McGovern\", 3) is 8 (Microsoft start_num example)",
    "tags": ["text", "find"],
    "formula": "=FIND(\"M\", \"Miriam McGovern\", 3)",
    "expected": { "number": 8 }
  },
  {
    "id": "fn.find-start-num-later-hit",
    "description": "start_num skips earlier matches: FIND(\"a\", \"banana\", 3) is 4",
    "tags": ["text", "find"],
    "formula": "=FIND(\"a\", \"banana\", 3)",
    "expected": { "number": 4 }
  },
  {
    "id": "fn.find-start-num-last",
    "description": "start_num on the last matching character still hits",
    "tags": ["text", "find"],
    "formula": "=FIND(\"a\", \"banana\", 6)",
    "expected": { "number": 6 }
  },
  {
    "id": "fn.find-start-num-past-last",
    "description": "start_num past the last match is #VALUE!",
    "tags": ["text", "find", "errors"],
    "formula": "=FIND(\"a\", \"banana\", 7)",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-start-num-zero",
    "description": "start_num 0 is #VALUE!",
    "tags": ["text", "find", "errors"],
    "formula": "=FIND(\"a\", \"abc\", 0)",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-start-num-neg",
    "description": "start_num -1 is #VALUE!",
    "tags": ["text", "find", "errors"],
    "formula": "=FIND(\"a\", \"abc\", -1)",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-start-num-trunc",
    "description": "start_num 2.9 truncates toward zero to 2",
    "tags": ["text", "find"],
    "formula": "=FIND(\"c\", \"abc\", 2.9)",
    "expected": { "number": 3 }
  },
  {
    "id": "fn.find-start-num-trunc-zero",
    "description": "start_num 0.9 truncates to 0 → #VALUE!",
    "tags": ["text", "find", "errors"],
    "formula": "=FIND(\"a\", \"abc\", 0.9)",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-start-num-true",
    "description": "TRUE coerces to start_num 1",
    "tags": ["text", "find", "coercion"],
    "quirks": ["bool-number-equality"],
    "formula": "=FIND(\"a\", \"abc\", TRUE)",
    "expected": { "number": 1 }
  },
  {
    "id": "fn.find-start-num-false",
    "description": "FALSE coerces to 0 → #VALUE!",
    "tags": ["text", "find", "errors", "coercion"],
    "quirks": ["bool-number-equality"],
    "formula": "=FIND(\"a\", \"abc\", FALSE)",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-start-num-text-num",
    "description": "Numeric text start_num is coerced",
    "tags": ["text", "find", "coercion"],
    "quirks": ["equality-vs-arithmetic-coercion"],
    "formula": "=FIND(\"b\", \"abc\", \"2\")",
    "expected": { "number": 2 }
  },
  {
    "id": "fn.find-start-num-text-bad",
    "description": "Non-numeric start_num is #VALUE!",
    "tags": ["text", "find", "errors"],
    "formula": "=FIND(\"a\", \"abc\", \"x\")",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-start-num-blank-cell",
    "description": "Blank start_num cell coerces to 0 → #VALUE!",
    "tags": ["text", "find", "errors", "empty"],
    "quirks": ["empty-cell-duality"],
    "formula": "=FIND(\"a\", \"abc\", A1)",
    "expected": { "error": "#VALUE!" },
    "workbook": {
      "sheets": [{ "name": "Sheet1", "cells": {} }]
    }
  },
  {
    "id": "fn.find-miss",
    "description": "Missing find_text is #VALUE! (not #N/A)",
    "tags": ["text", "find", "errors"],
    "formula": "=FIND(\"z\", \"abc\")",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-miss-substring",
    "description": "Partial leftover after start_num that cannot fit the needle is #VALUE!",
    "tags": ["text", "find", "errors"],
    "formula": "=FIND(\"an\", \"banana\", 5)",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-empty-find-text",
    "description": "Empty find_text matches at position 1",
    "tags": ["text", "find"],
    "formula": "=FIND(\"\", \"abc\")",
    "expected": { "number": 1 }
  },
  {
    "id": "fn.find-empty-find-text-start",
    "description": "Empty find_text with start_num returns start_num",
    "tags": ["text", "find"],
    "formula": "=FIND(\"\", \"abc\", 3)",
    "expected": { "number": 3 }
  },
  {
    "id": "fn.find-empty-find-text-len-plus-one",
    "description": "Empty find_text may land one past LEN(within_text)",
    "tags": ["text", "find"],
    "formula": "=FIND(\"\", \"abc\", 4)",
    "expected": { "number": 4 }
  },
  {
    "id": "fn.find-empty-find-text-past-end",
    "description": "Empty find_text with start_num > LEN+1 is #VALUE!",
    "tags": ["text", "find", "errors"],
    "formula": "=FIND(\"\", \"abc\", 5)",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-empty-both",
    "description": "FIND(\"\", \"\") is 1",
    "tags": ["text", "find"],
    "formula": "=FIND(\"\", \"\")",
    "expected": { "number": 1 }
  },
  {
    "id": "fn.find-empty-within",
    "description": "Non-empty needle in empty within_text is #VALUE!",
    "tags": ["text", "find", "errors"],
    "formula": "=FIND(\"a\", \"\")",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-blank-find-text-cell",
    "description": "Blank find_text cell coerces to \"\" and matches at 1",
    "tags": ["text", "find", "empty"],
    "quirks": ["empty-cell-duality"],
    "formula": "=FIND(A1, \"abc\")",
    "expected": { "number": 1 },
    "workbook": {
      "sheets": [{ "name": "Sheet1", "cells": {} }]
    }
  },
  {
    "id": "fn.find-case-sensitive-miss",
    "description": "FIND is case-sensitive (unlike SEARCH): \"a\" does not match \"ABC\"",
    "tags": ["text", "find"],
    "quirks": ["case-insensitive-text"],
    "formula": "=FIND(\"a\", \"ABC\")",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-case-sensitive-hit",
    "description": "FIND(\"A\", \"ABC\") is 1; SEARCH would also hit, but FIND requires exact case",
    "tags": ["text", "find"],
    "quirks": ["case-insensitive-text"],
    "formula": "=FIND(\"A\", \"ABC\")",
    "expected": { "number": 1 }
  },
  {
    "id": "fn.find-case-vs-search-mixed",
    "description": "FIND(\"bc\", \"ABC\") is #VALUE!; SEARCH would return 2 (case-insensitive)",
    "tags": ["text", "find"],
    "quirks": ["case-insensitive-text"],
    "formula": "=FIND(\"bc\", \"ABC\")",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-vs-search-wildcard-star",
    "description": "FIND treats * as a literal (SEARCH would treat it as a wildcard)",
    "tags": ["text", "find"],
    "quirks": ["wildcards"],
    "formula": "=FIND(\"*\", \"abc\")",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-vs-search-wildcard-pattern",
    "description": "FIND(\"a*\", \"abc\") is #VALUE!; SEARCH(\"a*\", \"abc\") would be 1",
    "tags": ["text", "find"],
    "quirks": ["wildcards"],
    "formula": "=FIND(\"a*\", \"abc\")",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-literal-star",
    "description": "A literal asterisk in within_text is found by FIND",
    "tags": ["text", "find"],
    "quirks": ["wildcards"],
    "formula": "=FIND(\"*\", \"a*b\")",
    "expected": { "number": 2 }
  },
  {
    "id": "fn.find-literal-question",
    "description": "? is a literal character, not a SEARCH single-char wildcard",
    "tags": ["text", "find"],
    "quirks": ["wildcards"],
    "formula": "=FIND(\"?\", \"a?b\")",
    "expected": { "number": 2 }
  },
  {
    "id": "fn.find-substring",
    "description": "Multi-character needle returns the first character position",
    "tags": ["text", "find"],
    "formula": "=FIND(\"an\", \"banana\")",
    "expected": { "number": 2 }
  },
  {
    "id": "fn.find-substring-start",
    "description": "start_num 3 skips the first \"an\" in banana",
    "tags": ["text", "find"],
    "formula": "=FIND(\"an\", \"banana\", 3)",
    "expected": { "number": 4 }
  },
  {
    "id": "fn.find-number",
    "description": "Numbers coerce to plain text before search; result is the 1-based index",
    "tags": ["text", "find", "coercion"],
    "quirks": ["equality-vs-arithmetic-coercion"],
    "formula": "=FIND(2, 12321)",
    "expected": { "number": 2 }
  },
  {
    "id": "fn.find-decimal",
    "description": "Decimal point is part of the coerced number text",
    "tags": ["text", "find", "coercion"],
    "formula": "=FIND(\".\", 12.5)",
    "expected": { "number": 3 }
  },
  {
    "id": "fn.find-bool",
    "description": "TRUE coerces to the text \"TRUE\"",
    "tags": ["text", "find", "coercion"],
    "formula": "=FIND(\"R\", TRUE)",
    "expected": { "number": 2 }
  },
  {
    "id": "fn.find-bool-needle",
    "description": "Boolean find_text coerces to \"TRUE\" / \"FALSE\"",
    "tags": ["text", "find", "coercion"],
    "formula": "=FIND(TRUE, \"TRUEBLUE\")",
    "expected": { "number": 1 }
  },
  {
    "id": "fn.find-unicode",
    "description": "UTF-8 scalar position (café): é is character 4, matching LEN/MID",
    "tags": ["text", "find"],
    "formula": "=FIND(\"é\", \"café\")",
    "expected": { "number": 4 }
  },
  {
    "id": "fn.find-arity-0",
    "description": "FIND() is #VALUE!",
    "tags": ["text", "find", "errors"],
    "formula": "=FIND()",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-arity-1",
    "description": "One argument is #VALUE!",
    "tags": ["text", "find", "errors"],
    "formula": "=FIND(\"a\")",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-arity-4",
    "description": "Four arguments is #VALUE!",
    "tags": ["text", "find", "errors"],
    "formula": "=FIND(\"a\", \"abc\", 1, 1)",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-err-find-text",
    "description": "Error in find_text propagates",
    "tags": ["text", "find", "errors"],
    "quirks": ["error-precedence"],
    "formula": "=FIND(1/0, \"abc\")",
    "expected": { "error": "#DIV/0!" }
  },
  {
    "id": "fn.find-err-within",
    "description": "Error in within_text propagates",
    "tags": ["text", "find", "errors"],
    "quirks": ["error-precedence"],
    "formula": "=FIND(\"a\", 1/0)",
    "expected": { "error": "#DIV/0!" }
  },
  {
    "id": "fn.find-err-start",
    "description": "Error in start_num propagates",
    "tags": ["text", "find", "errors"],
    "quirks": ["error-precedence"],
    "formula": "=FIND(\"a\", \"abc\", 1/0)",
    "expected": { "error": "#DIV/0!" }
  },
  {
    "id": "fn.find-err-left-wins",
    "description": "Left-to-right: find_text error beats a later #N/A",
    "tags": ["text", "find", "errors"],
    "quirks": ["error-precedence"],
    "formula": "=FIND(#DIV/0!, #N/A)",
    "expected": { "error": "#DIV/0!" }
  },
  {
    "id": "fn.find-err-na",
    "description": "NA() in find_text propagates #N/A",
    "tags": ["text", "find", "errors"],
    "formula": "=FIND(NA(), \"abc\")",
    "expected": { "error": "#N/A" }
  },
  {
    "id": "fn.find-err-cell",
    "description": "Error in a referenced cell propagates",
    "tags": ["text", "find", "errors"],
    "formula": "=FIND(A1, \"abc\")",
    "expected": { "error": "#VALUE!" },
    "workbook": {
      "sheets": [
        {
          "name": "Sheet1",
          "cells": {
            "A1": { "error": "#VALUE!" }
          }
        }
      ]
    }
  },
  {
    "id": "fn.find-omitted-start",
    "description": "Trailing-comma omitted start_num uses the default 1 (not Empty→0)",
    "tags": ["text", "find"],
    "formula": "=FIND(\"M\", \"Miriam McGovern\",)",
    "expected": { "number": 1 }
  },
  {
    "id": "fn.find-omitted-find-text",
    "description": "Omitted find_text coerces to \"\" and matches at 1",
    "tags": ["text", "find", "empty"],
    "formula": "=FIND(, \"abc\")",
    "expected": { "number": 1 }
  },
  {
    "id": "fn.find-omitted-within",
    "description": "Omitted within_text is empty: non-empty needle is #VALUE!",
    "tags": ["text", "find", "errors", "empty"],
    "formula": "=FIND(\"a\",)",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-omitted-both",
    "description": "FIND(,) is FIND(\"\",\"\") → 1",
    "tags": ["text", "find", "empty"],
    "formula": "=FIND(,)",
    "expected": { "number": 1 }
  },
  {
    "id": "fn.find-blank-within-cell",
    "description": "Blank within_text cell coerces to \"\" → #VALUE! for a non-empty needle",
    "tags": ["text", "find", "errors", "empty"],
    "quirks": ["empty-cell-duality"],
    "formula": "=FIND(\"a\", A1)",
    "expected": { "error": "#VALUE!" },
    "workbook": {
      "sheets": [{ "name": "Sheet1", "cells": {} }]
    }
  },
  {
    "id": "fn.find-stored-empty-within",
    "description": "Stored \"\" within_text is empty text → #VALUE! for a non-empty needle",
    "tags": ["text", "find", "errors"],
    "formula": "=FIND(\"a\", A1)",
    "expected": { "error": "#VALUE!" },
    "workbook": {
      "sheets": [
        {
          "name": "Sheet1",
          "cells": {
            "A1": { "text": "" }
          }
        }
      ]
    }
  },
  {
    "id": "fn.find-cell",
    "description": "FIND of a stored text cell uses the cell value",
    "tags": ["text", "find"],
    "formula": "=FIND(\"a\", A1, 3)",
    "expected": { "number": 4 },
    "workbook": {
      "sheets": [
        {
          "name": "Sheet1",
          "cells": {
            "A1": { "text": "banana" }
          }
        }
      ]
    }
  },
  {
    "id": "fn.find-whole-string",
    "description": "Needle equal to within_text is position 1",
    "tags": ["text", "find"],
    "formula": "=FIND(\"abc\", \"abc\")",
    "expected": { "number": 1 }
  },
  {
    "id": "fn.find-needle-longer",
    "description": "Needle longer than within_text is #VALUE!",
    "tags": ["text", "find", "errors"],
    "formula": "=FIND(\"abcd\", \"abc\")",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-start-num-len-plus-one-nonempty",
    "description": "Non-empty needle with start_num = LEN+1 is #VALUE! (empty find_text may land there)",
    "tags": ["text", "find", "errors"],
    "formula": "=FIND(\"a\", \"abc\", 4)",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-start-num-huge",
    "description": "start_num 1E+20 is far past LEN+1 → #VALUE!",
    "tags": ["text", "find", "errors"],
    "formula": "=FIND(\"a\", \"abc\", 1E+20)",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-overlapping",
    "description": "Overlapping needle: FIND(\"aa\", \"aaa\") is the first character",
    "tags": ["text", "find"],
    "formula": "=FIND(\"aa\", \"aaa\")",
    "expected": { "number": 1 }
  },
  {
    "id": "fn.find-overlapping-start",
    "description": "start_num 2 takes the second overlapping \"aa\" in \"aaa\"",
    "tags": ["text", "find"],
    "formula": "=FIND(\"aa\", \"aaa\", 2)",
    "expected": { "number": 2 }
  },
  {
    "id": "fn.find-literal-tilde",
    "description": "~ is a literal character (SEARCH would treat ~ as an escape)",
    "tags": ["text", "find"],
    "quirks": ["wildcards"],
    "formula": "=FIND(\"~\", \"a~b\")",
    "expected": { "number": 2 }
  },
  {
    "id": "fn.find-vs-search-tilde-escape",
    "description": "FIND(\"~*\", \"a*b\") is #VALUE!; SEARCH would match the escaped star",
    "tags": ["text", "find"],
    "quirks": ["wildcards"],
    "formula": "=FIND(\"~*\", \"a*b\")",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-literal-tilde-star",
    "description": "A literal ~* sequence in within_text is found by FIND",
    "tags": ["text", "find"],
    "quirks": ["wildcards"],
    "formula": "=FIND(\"~*\", \"a~*b\")",
    "expected": { "number": 2 }
  },
  {
    "id": "fn.find-case-later",
    "description": "Case-sensitive hit later in the string: FIND(\"B\", \"aaB\") is 3",
    "tags": ["text", "find"],
    "quirks": ["case-insensitive-text"],
    "formula": "=FIND(\"B\", \"aaB\")",
    "expected": { "number": 3 }
  },
  {
    "id": "fn.find-japanese",
    "description": "UTF-8 scalar position (日本語): 本 is character 2, matching LEN/MID",
    "tags": ["text", "find"],
    "formula": "=FIND(\"本\", \"日本語\")",
    "expected": { "number": 2 }
  },
  {
    "id": "fn.find-emoji-compat-v2",
    "description": "😀 is one Compatibility Version 2 scalar: FIND(\"😀\", \"x😀y\") is 2 (not UTF-16 unit 3)",
    "tags": ["text", "find"],
    "formula": "=FIND(\"😀\", \"x😀y\")",
    "expected": { "number": 2 }
  },
  {
    "id": "fn.find-after-emoji",
    "description": "Character after a supplementary-plane emoji is position 3",
    "tags": ["text", "find"],
    "formula": "=FIND(\"y\", \"x😀y\")",
    "expected": { "number": 3 }
  },
  {
    "id": "fn.find-combining-not-precomposed",
    "description": "Precomposed é does not match e + combining acute",
    "tags": ["text", "find"],
    "formula": "=FIND(\"é\", \"e\u0301\")",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-combining-mark",
    "description": "Combining acute is its own scalar at position 2",
    "tags": ["text", "find"],
    "formula": "=FIND(\"\u0301\", \"e\u0301\")",
    "expected": { "number": 2 }
  },
  {
    "id": "fn.find-unicode-start",
    "description": "start_num skips a leading multi-byte scalar: FIND(\"a\", \"éabc\", 2) is 2",
    "tags": ["text", "find"],
    "formula": "=FIND(\"a\", \"éabc\", 2)",
    "expected": { "number": 2 }
  },
  {
    "id": "fn.find-space",
    "description": "ASCII space is a literal character",
    "tags": ["text", "find"],
    "formula": "=FIND(\" \", \"a b\")",
    "expected": { "number": 2 }
  },
  {
    "id": "fn.find-tab",
    "description": "Tab (CHAR(9)) is found; space is not a tab",
    "tags": ["text", "find"],
    "formula": "=FIND(CHAR(9), \"a\"&CHAR(9)&\"b\")",
    "expected": { "number": 2 }
  },
  {
    "id": "fn.find-tab-not-space",
    "description": "Space does not match a tab in within_text",
    "tags": ["text", "find", "errors"],
    "formula": "=FIND(\" \", \"a\"&CHAR(9)&\"b\")",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-nbsp",
    "description": "NBSP CHAR(160) is not ASCII 32",
    "tags": ["text", "find"],
    "formula": "=FIND(CHAR(160), \"a\"&CHAR(160)&\"b\")",
    "expected": { "number": 2 }
  },
  {
    "id": "fn.find-quote",
    "description": "FIND locates a double quote in within_text",
    "tags": ["text", "find"],
    "formula": "=FIND(\"\"\"\", \"say \"\"hi\"\"\")",
    "expected": { "number": 5 }
  },
  {
    "id": "fn.find-percent",
    "description": "10% coerces to 0.1; FIND(\"1\", 10%) is 3",
    "tags": ["text", "find", "coercion"],
    "quirks": ["equality-vs-arithmetic-coercion"],
    "formula": "=FIND(\"1\", 10%)",
    "expected": { "number": 3 }
  },
  {
    "id": "fn.find-false-needle",
    "description": "FALSE coerces to the text \"FALSE\"",
    "tags": ["text", "find", "coercion"],
    "formula": "=FIND(FALSE, \"FALSEHOOD\")",
    "expected": { "number": 1 }
  },
  {
    "id": "fn.find-signed-zero",
    "description": "−0 formats as \"-0\"; FIND(\"-\", −0) is 1",
    "tags": ["text", "find", "coercion"],
    "formula": "=FIND(\"-\", -0)",
    "expected": { "number": 1 }
  },
  {
    "id": "fn.find-plus-zero",
    "description": "+0 formats as \"0\"; FIND(\"-\", 0) is #VALUE!",
    "tags": ["text", "find", "errors", "coercion"],
    "formula": "=FIND(\"-\", 0)",
    "expected": { "error": "#VALUE!" }
  },
  {
    "id": "fn.find-date-serial",
    "description": "DATE coerces to its serial text: DATE(1900,1,1) is 1",
    "tags": ["text", "find", "coercion"],
    "formula": "=FIND(\"1\", DATE(1900,1,1))",
    "expected": { "number": 1 }
  },
  {
    "id": "fn.find-nested-left",
    "description": "FIND(\"a\", LEFT(\"banana\", 3)) is 2",
    "tags": ["text", "find"],
    "formula": "=FIND(\"a\", LEFT(\"banana\", 3))",
    "expected": { "number": 2 }
  },
  {
    "id": "fn.find-nested-len",
    "description": "Empty find_text may land at LEN(within_text)+1",
    "tags": ["text", "find"],
    "formula": "=FIND(\"\", \"abc\", LEN(\"abc\")+1)",
    "expected": { "number": 4 }
  },
  {
    "id": "fn.find-nested-char",
    "description": "FIND(CHAR(65), \"ABC\") is 1",
    "tags": ["text", "find"],
    "formula": "=FIND(CHAR(65), \"ABC\")",
    "expected": { "number": 1 }
  },
  {
    "id": "fn.find-nested-rept",
    "description": "FIND(\"b\", REPT(\"a\", 10)&\"b\") is 11",
    "tags": ["text", "find"],
    "formula": "=FIND(\"b\", REPT(\"a\", 10)&\"b\")",
    "expected": { "number": 11 }
  },
  {
    "id": "fn.find-concat",
    "description": "FIND of concatenated pieces searches the joined text",
    "tags": ["text", "find"],
    "formula": "=FIND(\"c\", \"ab\"&\"cd\")",
    "expected": { "number": 3 }
  },
  {
    "id": "fn.find-plus-one",
    "description": "The result is a number: FIND(\"M\", \"Miriam McGovern\")+1 is 2",
    "tags": ["text", "find"],
    "formula": "=FIND(\"M\", \"Miriam McGovern\")+1",
    "expected": { "number": 2 }
  },
  {
    "id": "fn.find-iferror-miss",
    "description": "IFERROR wraps a missing needle",
    "tags": ["text", "find", "errors"],
    "formula": "=IFERROR(FIND(\"z\", \"abc\"), 0)",
    "expected": { "number": 0 }
  },
  {
    "id": "fn.find-array-literal",
    "description": "FIND of an array-literal needle scalarizes to the top-left",
    "tags": ["text", "find"],
    "formula": "=FIND({\"a\",\"z\"}, \"abc\")",
    "expected": { "number": 1 }
  },
  {
    "id": "fn.find-implicit-intersect",
    "description": "FIND(\"x\", A1:A3) from host B2 uses implicit intersection (same row → A2)",
    "tags": ["text", "find"],
    "quirks": ["implicit-intersection"],
    "at": "Sheet1!B2",
    "formula": "=FIND(\"x\", A1:A3)",
    "expected": { "number": 2 },
    "workbook": {
      "sheets": [
        {
          "name": "Sheet1",
          "cells": {
            "A1": { "text": "one" },
            "A2": { "text": "oxen" },
            "A3": { "text": "extra" }
          }
        }
      ]
    }
  },
  {
    "id": "fn.find-map-sum",
    "description": "SUM(MAP(array, LAMBDA(x, FIND(\"a\", x)))) adds first-\"a\" positions",
    "tags": ["text", "find", "map"],
    "formula": "=SUM(MAP({\"cat\";\"bat\";\"rat\"},LAMBDA(x,FIND(\"a\",x))))",
    "expected": { "number": 6 }
  }
]
